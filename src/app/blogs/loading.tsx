import Spinner from '@/components/customs/Spinner';

const LoadingPage = () => {
      return (
            <div>
                  <Spinner />
            </div>
      );
};

export default LoadingPage;